# Skill 使用說明

本專案目前有一個 skill：**kb-sync（知識庫同步）**，位置在 `.claude/skills/kb-sync/SKILL.md`。

---

## kb-sync：新資料與知識庫的比對整合

### 用途

把新的參考資料（Notion 匯出的 Markdown、會議記錄、隨手記等重點資訊），與現有知識庫 `docs/` 全文比對，找出差異並分類，有需要討論或衝突的地方先詢問我，確認後再整合進知識庫文件當中。

### 怎麼使用

把新資料放進 `原始參考資料/` 底下的日期資料夾（例如 `原始參考資料/20260815/`），然後對 Claude 說：

```
/kb-sync 原始參考資料/20260815
```

或直接用自然語言：

```
請參考 20260815 資料夾，分析出新的檔案與目前知識庫內容的差異
```

### 執行流程（五階段）

1. **讀取與前處理**：自動清理 Notion 匯出檔中的 base64 內嵌圖片，完整讀取新文件與全部現有 docs，原始檔案保持原樣不修改。
2. **逐節比對**：新文件的每一行都會歸入五類——已收錄／全新主題／舊主題新細節／衝突／敏感——任何一行都不能遺漏，數字會逐一驗算。
3. **報告差異並詢問**：輸出結構化差異報告。**遇到衝突（如兩邊金額不一致）或敏感內容（帳號密碼）會停下來先問我**，我裁決前不會動任何知識庫檔案。
4. **整合寫入**：動筆前先重新釐清整個知識庫架構——全新主題會思考放進哪個分類、排在哪個位置最合適再建新文件；舊主題的新內容會改寫融入既有文件的合適段落，讓整體閱讀與理解順暢，不是在文末貼補丁。新圖片會語意化重新命名搬入 `static/img/haomaiplus/`，並同步更新索引頁。
5. **驗證**：跑 `npm run build` 確認無壞連結、grep 確認無帳密外洩、回頭勾稽比對清單確認每個項目都已落地，最後回報所有變更。

### 建立此 skill 時的重點要素（原始需求）

> 讓我未來都可以順利的把類似 Notion 匯出的 Markdown 或者整理過的重點資訊，找尋出跟原有文件的差異，進行比較，有需要討論或者衝突的地方先詢問我，最後整合進入文件當中。
>
> - 所有重點都不遺漏
> - 撰寫前先重新釐清整個架構，有新的內容要思考怎麼加上去比較合適
> - 如果是舊的主題新的內容，也要確認清楚怎麼重新把內容整合進去，讓整體的閱讀及理解上是順暢的
> - 要讓之後的每一次產出，都可以有穩定的高品質表現

### 注意事項

- **帳號密碼一律不會寫進知識庫**（站台會公開 build），會改寫為「請向團隊管理者索取」。
- 原始參考資料夾永遠保持原樣，不會被修改或刪除。
- 名詞會自動正規化（例如物流商統一寫「日翊」，原始筆記常誤植為日意／日益）。
- 衝突項目沒有經過我裁決，不會自行選邊寫入。
